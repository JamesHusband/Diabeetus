# glucose-tracker

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build glucose-tracker` to build the library.

## Running unit tests

Run `nx test glucose-tracker` to execute the unit tests via [Jest](https://jestjs.io).
